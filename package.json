{
  "name": "obloop",
  "version": "0.1.0",
  "description": "OpenCode plugin: run a sequence of agents (e.g. backward → forward) as separate sessions, triggered by /obloop",
  "type": "module",
  "main": "obloop.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.3.13",
    "@types/node": "^22.0.0",
    "typescript": "~5.7.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*"
  }
}
